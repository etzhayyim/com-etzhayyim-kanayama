{:tasks
 {test {:doc "Run the complete standalone kanayama suite"
        :task (shell "clojure -M -m kanayama.test-runner")}
  audit {:doc "Validate canonical EDN and artifact boundaries"
         :task (shell "clojure -M scripts/audit.clj")}}}
